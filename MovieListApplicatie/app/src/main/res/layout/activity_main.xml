<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.kenneth.movielistapplicatie.MainActivity">

    <Button
        android:text="Nu in de cinema"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lastNewBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="27dp"
        android:id="@+id/cinemaBtn"
        android:onClick="cinemaClick"/>

    <Button
        android:text="Beste rating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/populairsteBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="42dp"
        android:id="@+id/ratingBtn"
        android:onClick="ratingClick"/>

    <Button
        android:text="populairste"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:id="@+id/populairsteBtn"
        android:layout_below="@+id/cinemaBtn"
        android:layout_alignRight="@+id/ratingBtn"
        android:layout_alignEnd="@+id/ratingBtn"
        android:onClick="populairsteClick"
        />

    <Button
        android:text="Binnenkort beschikbaar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ratingBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="37dp"
        android:id="@+id/binnenkortBeschikbaarBtn"
        android:onClick="binnenkortClick"/>
</RelativeLayout>
